import Foundation
import SDDSComponents

/// Зарезервировано под цепочки `GeneralAppearanceVariation` из ThemeBuilder, если появятся вложенные вариации.
/// Текущие корневые вариации заданы в `Carousel+BaseVariations` как `ComponentAppearanceVariation`.
