/* This file is code generated. */

import SwiftUI
import UIKit
@_exported import SDDSThemeCore

public extension SpacingToken {
    static var spacing0x: Self {
        let base = Self(
            value: 0.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing0x", base: base)
    }
    static var spacing10x: Self {
        let base = Self(
            value: 20.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing10x", base: base)
    }
    static var spacing12x: Self {
        let base = Self(
            value: 24.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing12x", base: base)
    }
    static var spacing16x: Self {
        let base = Self(
            value: 32.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing16x", base: base)
    }
    static var spacing1x: Self {
        let base = Self(
            value: 2.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing1x", base: base)
    }
    static var spacing20x: Self {
        let base = Self(
            value: 40.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing20x", base: base)
    }
    static var spacing24x: Self {
        let base = Self(
            value: 48.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing24x", base: base)
    }
    static var spacing2x: Self {
        let base = Self(
            value: 4.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing2x", base: base)
    }
    static var spacing32x: Self {
        let base = Self(
            value: 64.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing32x", base: base)
    }
    static var spacing3x: Self {
        let base = Self(
            value: 6.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing3x", base: base)
    }
    static var spacing40x: Self {
        let base = Self(
            value: 80.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing40x", base: base)
    }
    static var spacing4x: Self {
        let base = Self(
            value: 8.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing4x", base: base)
    }
    static var spacing60x: Self {
        let base = Self(
            value: 120.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing60x", base: base)
    }
    static var spacing6x: Self {
        let base = Self(
            value: 12.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing6x", base: base)
    }
    static var spacing8x: Self {
        let base = Self(
            value: 16.0
        )
        return ThemeTenantRegistry.shared.resolveSpacing(theme: "PlasmaB2C", id: "spacing8x", base: base)
    }
}
