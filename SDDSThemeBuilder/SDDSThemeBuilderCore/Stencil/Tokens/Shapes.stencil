/* This file is code generated. */

import SwiftUI
import UIKit
import SDDSThemeCore

public enum Shapes {
    {%- for key, value in json %}
    case {{ key }}
    {%- endfor %}

    public var token: ShapeToken {
        switch self {
            {%- for key, value in json %}
        case .{{ key }}:
            return .{{ key }}
            {%- endfor %}
        }
    }
}
