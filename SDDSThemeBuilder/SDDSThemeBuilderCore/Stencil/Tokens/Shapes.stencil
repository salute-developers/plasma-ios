/* This file is code generated. */

import SwiftUI
import UIKit
@_exported import SDDSThemeCore

{% if tenant %}
public extension Shapes {
    static func register{{ tenant }}() {
        ThemeTenantRegistry.shared.registerShapes(theme: "{{ themeName }}", tenant: Tenant(name: "{{ tenant }}")) { id in
            switch id {
            {%- for key, value in json %}
            case "{{ key }}": return .{{ key }}{{ tenant }}
            {%- endfor %}
            default: return nil
            }
        }
    }
}
{% else %}
public enum Shapes {
    {%- for key, value in json %}
    case {{ key }}
    {%- endfor %}

    public var token: ShapeToken {
        switch self {
            {%- for key, value in json %}
        case .{{ key }}:
            return ThemeTenantRegistry.shared.resolveShape(theme: "{{ themeName }}", id: "{{ key }}", base: .{{ key }})
            {%- endfor %}
        }
    }
}
{% endif %}
